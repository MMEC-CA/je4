# Workspace Access Guidelines

- **Read/Write Access:** Only the current project directory (`/workspaces/je4`) and the temporary directory (`/home/codespace/.gemini/tmp/je4`) are accessible for both reading and writing.
- **Restricted Access:** All other directories, including other projects under `/workspaces/`, are completely inaccessible and cannot be read from or written to.
